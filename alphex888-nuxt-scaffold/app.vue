<!--
This compatibility file is kept at the project root because the requested scaffold
listed `app.vue`. The active Nuxt source directory is configured as `app/`, so the
runtime entry is `app/app.vue`.
-->
<template>
  <NuxtPage />
</template>
