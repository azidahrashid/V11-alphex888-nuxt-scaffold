# Set this only when deploying to a GitHub Pages project subpath.
# Example: /your-repo-name/
NUXT_APP_BASE_URL=/

# Optional: force Nuxt/Nitro static preset in CI if preferred.
# NUXT_NITRO_PRESET=static
