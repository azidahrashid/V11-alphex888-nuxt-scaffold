{
  "name": "alphex888-nuxt-scaffold",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "migrate:extract": "node scripts/extract-site.mjs",
    "migrate:clean-css": "node scripts/clean-css.mjs",
    "migrate:convert": "node scripts/convert-to-nuxt.mjs",
    "migrate": "npm run migrate:extract && npm run migrate:clean-css && npm run migrate:convert"
  },
  "dependencies": {
    "@nuxtjs/google-fonts": "^3.2.0",
    "nuxt": "^3.15.0",
    "vue": "^3.5.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "playwright": "^1.49.0"
  }
}
