Nuxt components are stored in `app/components/` because `nuxt.config.ts` uses `srcDir: 'app/'`.

This folder is kept only to satisfy repository structure expectations and to avoid confusion when browsing the scaffold.
